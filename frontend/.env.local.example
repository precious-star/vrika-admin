# API URL when running server locally (no Docker)
INTERNAL_API_URL=http://127.0.0.1:8000

# Optional: Direct API access from browser (bypasses /be rewrite proxy)
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
